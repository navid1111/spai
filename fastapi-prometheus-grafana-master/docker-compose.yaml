version: "3.8"

services:
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    container_name: mysql
    environment:
      MYSQL_DATABASE: predictions_db
      MYSQL_USER: app_user
      MYSQL_PASSWORD: app_password
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - 3307:3306
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot_password"]
      interval: 10s
      timeout: 5s
      retries: 20
    networks:
      example-network:
        ipv4_address: 172.16.238.13

  app:
    build:
      context: ../
      dockerfile: fastapi-prometheus-grafana-master/app/Dockerfile
    restart: unless-stopped
    container_name: app
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      DATABASE_URL: mysql+pymysql://app_user:app_password@mysql:3306/predictions_db
      UPLOAD_DIR: /app/fastapi-prometheus-grafana-master/app/uploads
    volumes:
      - ./app/uploads:/app/fastapi-prometheus-grafana-master/app/uploads
    ports:
      - 8000:8000
    networks:
      example-network:
        ipv4_address: 172.16.238.10

  streamlit:
    build:
      context: ./streamlit
    restart: unless-stopped
    container_name: streamlit
    depends_on:
      - app
    environment:
      API_URL: http://app:8000
    ports:
      - 8501:8501
    networks:
      example-network:
        ipv4_address: 172.16.238.14

  prometheus:
    image: prom/prometheus:latest
    restart: unless-stopped
    container_name: prometheus
    ports:
      - 9090:9090
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      example-network:
        ipv4_address: 172.16.238.11

  grafana:
    image: grafana/grafana
    container_name: grafana
    restart: unless-stopped
    depends_on:
      - prometheus
    ports:
      - 3000:3000
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning
    env_file:
      - ./grafana/config.monitoring
    networks:
      example-network:
        ipv4_address: 172.16.238.12

networks:
  example-network:
    name: example-network
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.16.238.0/24

volumes:
  mysql_data:
